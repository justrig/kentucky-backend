let ioInstance;

function init(io) {
  ioInstance = io;
}

function enviarNotificacion(tipo, mensaje) {
  if (ioInstance) {
    ioInstance.emit("notificacion", { tipo, mensaje });
  }
}

module.exports = { init, enviarNotificacion };
